version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/back-end/
permissions:
  - object: /home/ec2-user/back-end/
    owner: root
    group: root
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: root